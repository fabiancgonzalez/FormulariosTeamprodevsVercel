{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "angular",
  "env": [
    "MONGODB_URI"
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
